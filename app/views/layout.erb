<!DOCTYPE html>
<html lang="en">
<head>
  <title>Meetups</title>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/normalize.css" />
  <link rel="stylesheet" href="/stylesheets/foundation.min.css" />
  <link rel="stylesheet" href="/stylesheets/app.css" />
  <script src="/javascripts/hide_button.js"></script>
</head>
<body>
  <!-- Start Top Bar -->
  <div class="title-bar" data-responsive-toggle="menu" data-hide-for=medium>
    <button class="menu-icon" type="button" data-toggle></button>
    <div class="title-bar-title">Menu</div>
  </div>

  <div class="top-bar" id="menu">
    <div class="top-bar-left">
      <ul class="dropdown menu" data-dropdown-menu>
        <li class="menu-text"><a href="/meetups" class="site-name">Meetups in Space</a></li>
      </ul>
    </div>
    <div class="top-bar-right">
      <ul class="menu">
        <% if current_user %>
        <li class="menu-text">Signed in as <%= current_user.username %></a></li>
        <li class="menu-text"><a href="/sign_out">Sign Out</a></li>
        <% else %>
        <li class="menu-text"><a href="/auth/github">Sign In</a></li>
        <% end %>
      </ul>
    </div>
  </div>
  <!-- End Top Bar -->

  <% if flash.any? %>
    <div class="flash">
      <div class="container">
        <%= flash[:notice] %>
      </div>
    </div>
  <% end %>

  <% errors_exist = defined? @errors %>
  <% if errors_exist != nil %>
  <% if  @errors.length > 0 %>
    <div class="flash">
      <div class="container">
        <% @errors.each do |error| %>
          <%= error %> <br>
        <% end %>
      </div>
    </div>
  <% end %>
  <% end %>

  <div class= "row column">
    <%= yield %>
  </div>

  <script src="/javascripts/jquery.js"></script>
  <script src="/javascripts/foundation.min.js"></script>
  <script src="/javascripts/app.js"></script>
  <script src="/javascripts/what-input.js"></script>
</body>
</html>
